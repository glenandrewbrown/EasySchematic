/** LAYPAL block-colour quick-assign palette (board 5c) — the 9 curated hues offered on every
 *  colour swatch row (inspector, context menu, layers, sheet). Kept in its own module so the
 *  ColorSwatchRow component file only exports a component (react-refresh). */
export const LAYPAL_COLORS: readonly string[] = [
  "#3d8bfd",
  "#1fb6a6",
  "#e0a345",
  "#c879e8",
  "#e5645f",
  "#46c89a",
  "#ef7a72",
  "#6f86e0",
  "#74819a",
];
